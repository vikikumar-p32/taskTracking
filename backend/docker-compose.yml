services:
  db:
    image: postgres:latest
    container_name: postgres_db
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: HareKrsna#123
      POSTGRES_DB: task_tracking_db
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
